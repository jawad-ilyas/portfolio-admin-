import React from 'react'
import { useSelector, useDispatch } from "react-redux"
import { removeTodo } from '../features/todo/todoSlice'
function Todos() {

    // const selecter = useSelector(state => state.todos)
    // console.log("selecter value into the todos admin", selecter)
    // return (
    //     <div>
    //         {selecter?.map((item, index) => {
    //             return <div key={index}>{item.name}<br />{item.description}</div>
    //         })}
    //     </div>
    // )
}

export default Todos